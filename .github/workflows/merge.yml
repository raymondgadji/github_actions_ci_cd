name: Exercice Merge message

on:
  workflow_dispatch:
  pull_request:
    types: [closed]

jobs:
  say-merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 PR (pull request) #${{ github.event.pull_request.number }} mergée ! Merci @${{ github.event.pull_request.user.login }}"